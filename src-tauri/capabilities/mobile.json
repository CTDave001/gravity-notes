{
  "$schema": "../gen/schemas/mobile-schema.json",
  "identifier": "mobile",
  "description": "Permissions for the single-window iOS application",
  "platforms": ["iOS"],
  "windows": ["main"],
  "permissions": [
    "core:default",
    "store:default",
    "dialog:allow-save",
    "fs:allow-write-file"
  ]
}
